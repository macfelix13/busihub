# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/test-results
/playwright-report

# next.js
/.next/
/out/

# production
/build

# env files — never commit real secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# misc
.DS_Store
*.pem
*.log
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase local
supabase/.branches
supabase/.temp

# local Claude Code / Cowork tooling config — not part of the app
.github/agents/
.github/hooks/
.github/skills/

# one-off delivery scripts used to apply Claude-generated changes locally —
# not application code, shouldn't accumulate in history
apply-*.ps1
fix-*.ps1
busihub-security-audit.md
