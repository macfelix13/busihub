Fix notification bell dropdown clipping off-screen on mobile

The panel was anchored right-0 to its own button, which only works if
that button sits at the true right edge of the screen - it doesn't,
Sign out sits to its right in the header. Anchors to the viewport's own
edges below the tablet breakpoint instead, where this actually mattered.
Found from a real phone-width screenshot rather than reading the code.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EWx2YxrH2vH9gULYahXRAu
